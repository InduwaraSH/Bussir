import 'package:flutter/material.dart';

class Ongoingscreen extends StatelessWidget {
  const Ongoingscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("ghgh")),
    );
  }
}
